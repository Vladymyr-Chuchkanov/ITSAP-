@model IEnumerable<TouristWeb0.RankPartisipant>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.DateOfAchievement)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Partisipant)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Rank)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.DateOfAchievement)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Partisipant.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Rank.Name)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.RankPartisipantId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.RankPartisipantId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.RankPartisipantId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
