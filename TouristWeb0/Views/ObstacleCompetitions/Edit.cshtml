@model TouristWeb0.ObstacleCompetition

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>ObstacleCompetition</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="ObstacleCompetitionId" />
            <div class="form-group">
                <label asp-for="ObstacleId" class="control-label"></label>
                <select asp-for="ObstacleId" class="form-control" asp-items="ViewBag.ObstacleId"></select>
                <span asp-validation-for="ObstacleId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="CompetitionId" class="control-label"></label>
                <select asp-for="CompetitionId" class="form-control" asp-items="ViewBag.CompetitionId"></select>
                <span asp-validation-for="CompetitionId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
